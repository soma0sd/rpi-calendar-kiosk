# RPi 로 그대로 scp 되는 셸 스크립트는 반드시 LF 로 둔다.
# CRLF 로 전송되면 원격 bash 가 `set -euo pipefail\r` 을 잘못된 옵션으로 읽고 실패한다.
*.sh text eol=lf

# systemd 유닛도 Linux 에서 그대로 읽히므로 LF 고정.
*.service text eol=lf
*.timer text eol=lf
